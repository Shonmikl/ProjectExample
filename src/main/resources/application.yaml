server:
    port: 8080

spring:
    main:
        banner-mode: off

    datasource:
        driverClassName: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/proEx
        username: root
        password: 123123

    jpa:
        show-sql: true
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: none
            
    liquibase:
        change-log: classpath:db/changeLog/changelog-master.xml
        enabled: true